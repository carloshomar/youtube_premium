import { env } from '../config/env';

export const themeColors = {
  bg: env.theme.bg,
  surface: env.theme.surface,
  chip: env.theme.chip,
  muted: env.theme.muted,
  red: env.theme.red,
  white: env.theme.white,
  border: env.theme.border,
};
